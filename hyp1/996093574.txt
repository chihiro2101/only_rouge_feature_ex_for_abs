the class is no longer coupled to a particular request and has no knowledge ( is independent ) of how the request is carried out if the program embeds a scripting engine , each command object can implement a method , and user actions can then be easily recorded as scripts a source/invoker object calls the command/action object 's execute/performaction method as a result , the command object no longer contains a reference to the target object nor a reference to the application code 